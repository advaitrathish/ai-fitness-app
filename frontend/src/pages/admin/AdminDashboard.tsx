import StubPage from "@/components/StubPage";
import { LayoutDashboard } from "lucide-react";
const AdminDashboard = () => <StubPage title="Admin Dashboard" description="Overview of system health, user activity, and key metrics." icon={LayoutDashboard} />;
export default AdminDashboard;
