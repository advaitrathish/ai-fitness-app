import StubPage from "@/components/StubPage";
import { CreditCard } from "lucide-react";
const AdminRevenue = () => <StubPage title="Revenue & Premium Analytics" description="Track subscription revenue, conversion rates, and premium user metrics." icon={CreditCard} />;
export default AdminRevenue;
