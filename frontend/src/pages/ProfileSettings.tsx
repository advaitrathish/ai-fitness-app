import StubPage from "@/components/StubPage";
import { Settings } from "lucide-react";
const ProfileSettings = () => <StubPage title="Profile Settings" description="Manage your account, preferences, and notification settings." icon={Settings} />;
export default ProfileSettings;
